/**
 * Sidebar / dashboard header mark: `frontend/public/branding/logo.png`.
 */
export const BRANDING_LOGO_URL = '/branding/logo.png';

/**
 * Marketing navbar, auth header, and mobile dashboard bar use the same asset as the browser tab icon.
 * File: `frontend/public/favicon.png`. Bump `v` here and in `index.html` when you replace the PNG.
 */
export const FAVICON_URL = '/favicon.png?v=3';
