/**
 * Booking table composition is currently handled inside the bookings page
 * with AppDataTable column definitions. Keep this as an extraction point.
 */
export function BookingTable() {
  return null;
}

